---
import { Image } from 'astro:assets';
import Button from '../ui/Button.astro';
import heroBgImg from '@assets/photo-1586528116311-ad8dd3c8310d.jpg';
---

<section class="section-bg-dark relative h-screen min-h-[600px] flex items-center overflow-hidden">
  <!-- Background Image with Overlay -->
  <div class="absolute inset-0">
    <Image 
      src={heroBgImg}
      alt="Modern logistics warehouse with advanced automation"
      class="w-full h-full object-cover"
      width={2070}
      height={1380}
      decoding="async"
      fetchpriority="high"
    />
    <div class="absolute inset-0 bg-linear-to-r from-slate-900/95 via-slate-900/80 to-slate-900/50"></div>
  </div>

  <!-- Content -->
  <div class="container mx-auto relative z-10 px-4 sm:px-6 lg:px-8">
    <div class="max-w-4xl">
      <div class="pill-gold mb-6 flex items-center">
    
        Fuar & Etkinlik Çözümleri
      </div>
      
      <h1 class="heading-hero mb-6">
        Projelerinizi
        <span class="block text-gold mt-2">
          Tanas Fuarcılık
        </span>
        ile hayata geçirin
      </h1>
      
      <p class="text-body text-body-secondary mb-8 max-w-2xl">
        Fuar standı, kongre ve tanıtım projelerinde; tasarımdan üretime, kurulumdan söküme kadar tüm süreci uçtan uca yönetiyoruz.
      </p>
      
      <div class="flex flex-col sm:flex-row gap-4 mb-12">
        <Button href="/rfq" size="lg" class="btn-primary">
          Teklif Al
        </Button>
        <Button href="/capabilities" size="lg" variant="outline" class="btn-primary">
          Hizmetleri İncele
        </Button>
      </div>

      <!-- Quick Stats -->
      <div class="grid grid-cols-2 md:grid-cols-4 gap-6 pt-8 border-t border-white/20">
        <div class="group">
          <div class="text-stat text-gold mb-1">16+ <br>Yıl</div>
          <div class="text-label text-slate-300 group-hover:text-gold transition-colors">Deneyim</div>
        </div>
        <div class="group">
          <div class="text-stat text-gold mb-1">Yüzlerce <br>Proje</div>
          <div class="text-label text-slate-300 group-hover:text-gold transition-colors">Uygulama</div>
        </div>
        <div class="group">
          <div class="text-stat text-gold mb-1">Zamanında <br>Kurulum</div>
          <div class="text-label text-slate-300 group-hover:text-gold transition-colors">Teslim</div>
        </div>
        <div class="group">
          <div class="text-stat text-gold mb-1">Anahtar <br>Teslim</div>
          <div class="text-label text-slate-300 group-hover:text-teal-300 transition-colors">Hizmet</div>
        </div>
      </div>
    </div>
  </div>

  <!-- Scroll indicator -->
  <div class="absolute bottom-8 left-1/2 -translate-x-1/2 animate-bounce">
    <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
    </svg>
  </div>
</section>

